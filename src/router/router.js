import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router);

const routes = [{
    path: '/',
    name: 'xxx',
    component: 'xxx'
}]

const router = {
    routes,
    mode: 'history'
}

export default router;